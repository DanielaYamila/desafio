<div>
    <h2>Lista de productos</h2>
    <div{{#each products.product}}>
        <p>{{this.title}}</p>
        <p>{{this.price}}</p>
        <img src={{this.image}}>
        <br>
    </div {{/each}}>
    <a href="http://localhost:8080/"><button>Agregar Productos</button></a>
</div>